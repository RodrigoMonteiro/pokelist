import { TransformWeightPipe } from './transform-weight.pipe';

describe('TransformWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
