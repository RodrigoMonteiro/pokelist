import { TransformHeightPipe } from './transform-height.pipe';

describe('TransformHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
